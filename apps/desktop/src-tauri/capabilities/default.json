{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Restrictive default permissions for local permission editing.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
